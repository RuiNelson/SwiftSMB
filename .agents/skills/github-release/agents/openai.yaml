interface:
  display_name: "GitHub Release"
  short_description: "Prepare and publish GitHub releases"
  default_prompt: "Use $github-release to prepare and publish a GitHub release for this repository."
